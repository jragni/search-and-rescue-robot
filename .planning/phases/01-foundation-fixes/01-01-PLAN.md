---
phase: 01-foundation-fixes
plan: 01
type: execute
---

<objective>
Fix data structure and logic bugs in the SearchAndRescue action server.

Purpose: These bugs cause immediate runtime crashes or incorrect logic that prevents the mission from running at all.
Output: Action server can detect humans and correctly track them without crashing.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/CONCERNS.md
@colcon_ws/src/transbot_bringup/scripts/search_and_rescue_action_server_old.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix dict vs set type mismatch</name>
  <files>colcon_ws/src/transbot_bringup/scripts/search_and_rescue_action_server_old.py</files>
  <action>
    Line 33: Change `self.human_poses = {}` to `self.human_poses = set()`.
    Line 34: Change `self.visited_human_poses = {}` to `self.visited_human_poses = set()`.

    The code uses `.add()` on line 100 and `.pop()` on line 134, which are set methods.
    The comment on line 33 already indicates "Set of human poses" so the intent was always a set.
  </action>
  <verify>python3 -c "s = set(); s.add((1,2,3)); print(s.pop())"</verify>
  <done>Lines 33-34 use set() initialization. No more AttributeError on .add() or .pop() calls.</done>
</task>

<task type="auto">
  <name>Task 2: Fix enum comparison bug</name>
  <files>colcon_ws/src/transbot_bringup/scripts/search_and_rescue_action_server_old.py</files>
  <action>
    Line 76: Change `self.current_task != [MissionTask.SEARCHING]` to `self.current_task != MissionTask.SEARCHING`.

    The current code compares an enum to a list containing an enum, which is always True.
    This causes all human detections to be ignored since is_not_searching is always True.
  </action>
  <verify>python3 -c "from enum import Enum; class T(Enum): A=1; t=T.A; print(t != T.A, t != [T.A])"</verify>
  <done>Line 76 compares enum directly. Output: "False True" showing direct comparison works correctly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Both changes made to the file
- [ ] No Python syntax errors: `python3 -m py_compile colcon_ws/src/transbot_bringup/scripts/search_and_rescue_action_server_old.py`
- [ ] set() initialization present on lines 33-34
- [ ] Direct enum comparison on line 76
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No new errors introduced
- Data structures correctly typed
- Enum comparison logic correct
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-fixes/01-01-SUMMARY.md`
</output>
